# AIによる変更管理ポリシー

## 必須ゲート
1. 変更目的を明文化
2. 影響範囲を列挙
3. 既存ファイルとの差分を提示
4. GitHubへバックアップまたはブランチ作成
5. テスト計画を提示
6. 人間承認後に適用

## 禁止
- 既存レイアウトの勝手な全面刷新
- 既存Portalカードの削除
- Dockerボリューム削除
- DBスキーマ変更の即時適用
- `docker compose down -v` の自動実行

## 大規模変更時のルール
大規模変更は必ず `proposal/` に差分案として保存し、実ファイルには直接反映しない。
