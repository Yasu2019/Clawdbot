# メカ歩行RL 階段昇段 解決 引き継ぎ (2026-08-15)

対象: `projects/AtsugiMechaCity/rl_integration/stage_a`(Genesis + rsl_rl PPO)
状態: **階段10段の完全踏破を達成。平地歩行は過去最高品質。**

---

## 1. 到達点

| 指標 | セッション開始時 | 現在 (v30) |
|---|---|---|
| 階段 survival | **0.0** | **0.713〜0.904** |
| 登った段数 | 0 段(階段に到達すらせず) | **平均 9.51 / 最大 10 段(全段)** |
| height_gained | -0.078 m | **+1.064〜1.065 m**(全速度で完登) |
| 15秒完走率 | — | **87.9 %** |
| 転倒(tilt)率 | — | 16.0 % → **6.2 %** |
| 潰れ(low)率 | — | **5.9 %**(9段目付近、1.6%まで低下) |
| 平地 survival | 0.94 | **1.000**(全速度) |
| 平地 単脚支持率 | 0.61〜0.67 | **0.914〜0.921**(人間基準0.8超) |

`survival 0.0` が3週間動かなかった課題が解決した。

### 検証済みチェックポイント

| ファイル | 内容 |
|---|---|
| `known_good/walk_rsl_stairs_v30_20260815_tilt6pct_climb9.51.pt` | **最良**。9.51段 / survival 0.713-0.904 |
| `known_good/walk_rsl_stairs_v28_20260814_FULL10steps_climb9.23.pt` | 初の全段完登 |
| `known_good/walk_rsl_flat_v26_20260814_60kg_surv1.00_1foot0.92.pt` | 新機体の平地基準(warm-start元) |

いずれも `verify_policy.py` フレッシュ再ロード + フレーム目視の両方で確認済み。

---

## 2. 真因は報酬ではなく「機体が自重を支えられない」ことだった

**最重要**。20回以上の報酬調整で動かなかったものが、機体を直した1回で解決した。

決定的だった実測: **平地でゼロ行動(既定姿勢の保持指令のみ)でも1.5秒で胴体zが
0.428m → 0.055m まで潰れる**。方策以前の問題だった。

### (1) 質量が非物理的だった

カプセル体積から逆算した密度:

    upper_leg   8470 kg/m3   <- 鉄鋼 7850 を超える
    lower_leg  10032 kg/m3   <- 鉛 11340 に迫る

2026-07-10 のトップヘビー対策(torso ×0.7 / 脚 ×1.4)が、元から重い脚をさらに
重くしていた。**総質量238kgは保存される設計だったため、総量だけ見ても気づけなかった。**

実在ヒューマノイドとの比較(当機体はリンク全長 1.40m 相当):

| 機体 | 身長 | 質量 | kg/m |
|---|---|---|---|
| Unitree G1 | 1.27 m | 35 kg | 27.6 |
| Unitree H1 | 1.80 m | 47 kg | 26.1 |
| Honda ASIMO | 1.30 m | 54 kg | 41.5 |
| BD Atlas | 1.50 m | 89 kg | 59.3 |
| **当機体(旧)** | 1.40 m | **238 kg** | **170.0** |

### (2) PDゲインが不足していた

KP/質量が **1.26**(G1 は **4.29**)。単脚支持の重力トルク(モーメント腕0.10mで
233 N·m)にPDが釣り合うには膝の偏差 44.6° が必要だが、膝の可動域は 40° しかない。
膝が限界まで折れて支えられない。追従誤差が全区間 20〜80° だったのはこのため。

### 対処

`train_v50_walk_tracking.py` の2箇所のみ:

- `MASS_REDIST` に `_MASS_SCALE=0.2521` を掛け **238.0 → 60.0 kg**
  (重心を下げる ×0.7/×1.4 の比は保持。腕は従来等倍だったので明示的に係数付与)
- `KP` を G1 の KP/質量比 4.29 に合わせ一律 ×0.857(膝 300→257)
- `KV` は減衰比 ζ ∝ KV/√(KP·m) を保存し ×0.465

修正後、ゼロ行動4秒で潰れず(胴体z 0.40-0.57 維持、upright 0.978-0.992)。

---

## 3. 本セッションで修正した構造的欠陥(全7件)

いずれも**測定して初めて分かった**もので、報酬係数の推測調整では到達できない。

| # | 欠陥 | 発見方法 | コミット |
|---|---|---|---|
| 1 | `double_support_ratio` の符号反転(両足接地を報酬していた) | `--reward-breakdown` の実測 | `dc100d184c` |
| 2 | `climb_stagnation` 過剰(-20.0、早期転倒を誘発) | 報酬内訳で支配的と判明 | `092ac53c27` |
| 3 | 地形高さの基準が胴体(階段で未踏の段を基準に誤判定) | 転倒判定の時系列実測 | `25c28d27e1` |
| 4 | 指令速度が遅すぎて階段に到達不能(階段用報酬が一度も発火せず) | 地形形状の実測 | `0fe1e3ce50` |
| 5 | `alive` 報酬の欠落(転んだ方が数値上得だった) | G1 設定との機械比較 | `68c80d70bf` |
| 6 | **機体が自重を支えられない(238kg / KP質量比1.26)** | **ゼロ行動保持テスト** | `591b9b7431` |
| 7 | `orientation` が G1 の5倍厳しくバランス回復の傾きを罰していた | 終了理由の内訳実測 | `e401d4a7f9` |

### 自分で作り込んで直したバグ(記録として)

- `double_support_ratio`(#1)は本セッションで私が追加した項。負を返す関数に負の係数を
  掛け、7日間「立ち尽くし」を強化していた。
- `com_over_lead_foot` を `com_lateral_track` の `exp(-err²/0.03)` 形で実装したが、
  これは左右の数cm誤差向けの値で、前後方向の実測誤差 0.57m では報酬 2.2e-05・
  勾配ほぼゼロで**完全に死んでいた**。Cauchy 形 `1/(1+err²/0.3²)` へ修正(`b7cc56a1b5`)。

---

## 4. 測定で分かった「効いていない報酬項」

現行方策の全報酬項の寄与を実測した結果(`diag_rewards.py`):

    上位8項で全体の 83 %
    31項中 19項が寄与 1% 未満

実質的に効いているのは8項のみ:

| 項 | 寄与 | シェア |
|---|---|---|
| `alive` | +0.15000 | 27.6 % |
| `climb_progress` | +0.09554 | 17.5 % |
| `forward_progress` | +0.05050 | 9.3 % |
| `gait_symmetry` | -0.04292 | 7.9 % |
| `com_over_lead_foot` | +0.03493 | 6.4 % |
| `tracking_lin_vel` | +0.02927 | 5.4 % |
| `com_lateral_track` | +0.02651 | 4.9 % |
| `climb_stagnation` | -0.02548 | 4.7 % |

G1 との差分として検証候補にしていた項は、**測定の結果すべて寄与ゼロ〜0.2%**だった:

    feet_air_time       -0.00134 (0.2%)   G1は0.0(無効)
    collision           +0.00000 (0.0%)   一度も発火していない
    dof_vel             -0.00011 (0.0%)   G1は10倍強い
    single_foot_contact +0.01351 (2.5%)   G1に無い

→ **これらを変更しても測定可能な差は出ない**(学習を回す前に測って数時間節約した)。

**次にやるなら**: 寄与1%未満の19項の整理。効果が無いのにバグの温床になっている
(今日混入したバグ2件はどちらも低寄与の項だった)。G1が15項で済ませているのは
理にかなっている。ただし既存動作を壊すリスクがあるため慎重に。

---

## 5. 既知の性質(未対処・費用対効果で見送り)

`_support_terrain_dz()` は接地足の **最大値** を取る。1段目と3段目に跨いだ瞬間に
基準高さが 0.2m 跳ね、胴体が追いつく前に `term_low` が立つ。

実測: `term_low` 発火 4件/256env(1.6%)のうち約半分がこれ。発火位置は平均9.0段目
(ほぼ頂上)。期待効果が小さく、`base_height` 報酬も同じ関数を使うため再検証が
必要になることから見送った。

---

## 6. 方法論(最も再利用価値が高い)

### RL学習が進まないとき、報酬を触る前に測る4項目

1. **静的保持テスト**: ゼロ行動(既定姿勢の保持指令のみ)で自重を支えられるか。
   潰れるなら方策以前の問題。
2. **質量・寸法の妥当性**: 実在機と kg/m 等の無次元量で比較する。
   **総質量だけ見ても再配分バグは見つからない**ので、必ずリンク体積から密度を逆算する
   (鉄鋼 7850 を超えたら異常)。
3. **KP/質量比**: 実績のある機体(unitree_rl_gym G1 は 4.29)と同オーダーか。
4. **タスクの到達可能性**: 目標地物までの距離 ÷ 指令速度 < エピソード長 か。
   満たさなければ、その地形用の報酬は一度も発火しない。

### 外部調査は「公開実装の設定ファイルとの数値比較」だけが効く

- **効いた**: `unitree_rl_gym` の `g1_config.py` と `reward_scales` / `stiffness` を
  機械比較 → `alive` 欠落と KP/質量比の不足を発見。実在機の公表スペックとの
  kg/m 比較 → 質量が3〜6倍過大と判明(3週間の停滞の真因)。
- **効かなかった**: 生体力学文献から5ルールを自前実装(2026-07-27)、CMUモーション
  キャプチャの導入。原理を報酬項へ翻訳する過程でバグが入る(本セッションで2件混入)。
- 象徴的な例: 単脚支持率は文献調査+5ルール実装では 0.35-0.67 のまま動かなかったが、
  **質量を直したら報酬調整なしで 0.92** になった。

参照リポジトリは `D:\RobotAI\repos\` に clone 済み(`legged_gym` / `unitree_rl_gym`、
参照専用)。

### 判定は必ず実測で行う

学習時の `return` はタスク成功の指標にならない。実例:

- v19 継続で `return` 19.6 → 28.5 と上昇したが、登坂成功率は 85.2% → 13.3% に激減。
- 良好なチェックポイントは**その場で** `known_good/` へ退避すること
  (`latest.pt` は上書きされ、RL は単調に改善しない)。本セッションで2回救われた。

判定に使う実測スクリプト(`D:\Temp\...\scratchpad\` に残置、必要なら再利用):

- `diag_climb.py` — 何段登れたか / 生存時間 / 到達距離
- `diag_straddle.py` — 片足/両足がどの段に乗ったか、終了時の状態
- `diag_rewards.py` — 全報酬項の寄与
- `diag_lowfall.py` — 終了理由の内訳と `term_low` 発火時の状態

---

## 7. 次にやるなら

| 候補 | 内容 | 備考 |
|---|---|---|
| 降段・不整地への展開 | 同じ機体で `stairs_down` / `slope` / corridor | 旧チェックポイントは全て無効(質量変更のため)。平地から積み直しが必要 |
| 報酬項の整理 | 寄与1%未満の19項を削減 | バグ面積の削減が目的。要慎重 |
| 人間らしさの向上 | AMP(敵対的モーション事前分布)等 | 現行 `pose_prior` は関節角度テーブル追従で模倣としては最も弱い形式 |
| 外乱耐性 | push有りでの評価・学習 | 現在の実測は無擾乱。学習時は push 有り |

**注意**: 質量変更により、2026-08-10 以前の `known_good` チェックポイントは
**すべて無効**(ダイナミクスが別物)。`README_INVALID_20260808.md` と同じ状況が
再発しているため、旧ファイルを再利用しないこと。

---

## 8. 記録先

本セッションの知見は `record_finding.py` で4系統へ記録済み(各 4/4 成功):

- Beads(`bd remember`)
- Obsidian `data/workspace/obsidian_vault/トラブルシューティング/`
- auto-memory `~/.claude/projects/D--Clawdbot-Docker-20260125/memory/`(MEMORY.md 索引更新済み)
- ByteRover(`brv curate`)

`trouble_history.md` には T079i / T079n / T079r を追記。
GitHub へプッシュ済み。

**未記録**: Turso(メール用途専用のため対象外)、graphify(hookが毎回600秒で
タイムアウトし manifest が 2026-07-30 から未更新。別途対処が必要な既知の未解決事項)。

---

## 追記 (2026-08-17): 外乱条件下の実態と、push カリキュラムによる改善

### 上記 §1 の survival は「無擾乱」条件の値だった(重要な但し書き)

学習時は push 0.35 m/s(5秒毎)+ DR(質量・ゲイン ±10%)が入るが、評価は
push_vel=0.0 / DR無効で行っていた。同条件で測り直すと大きく違う。

### 現在の最良 (v33, push カリキュラム)

| 条件 | v30 (§1の値) | v32 (一定push 6000iter) | **v33 (カリキュラム 3000iter)** |
|---|---|---|---|
| 無擾乱 | 0.852 | 0.635 | **0.896** |
| push 0.35 | 0.488 | 0.418 | **0.639** |
| **push + DR(学習時と同条件)** | **0.143** | 0.227 | **0.744** |
| height_gained | +1.065 m | +1.074 m | **+1.075 m** |

`VERIFIED: known_good/walk_rsl_stairs_v33_20260817_pushDR0.744_CURRICULUM.pt`

### 効いた対策: push 強度のカリキュラム

`--push-curriculum-frac F` を追加(`train_v50_walk_rsl.py`)。push_vel を学習の
最初の F 割で 0 → 設定値まで線形に上げ、以後維持する。既定 0.0 で無効。

決定的だったのは学習曲線の質的な違い:

    v32(一定push)   it1000 fall 0.95 → it3000 fall 0.97   横ばい
    v33(curriculum) it1000 fall 1.00 → it3000 fall 0.85   最後まで伸び続けた

**時間ではなく「与える順序」の問題だった。** v32 は 6000iter で 0.227 止まり、
v33 は 3000iter で 0.744。

### 効かなかった対策(記録)

- **長時間学習 (v32, 6000iter)**: 1000iter 以降は横ばい。5000iter 分が無効。
  push+DR は 0.227 へ改善したが無擾乱が 0.635 へ悪化するトレードオフ。
- **roll 解放 (v31)**: push 下で ankle_roll が可動域の65%で飽和する一方 hip_roll に
  余裕があったため、`pose_prior` から roll 4DOF を除外して股関節の使用を促した。
  **全条件で悪化**(0.852→0.652 / 0.488→0.322 / 0.143→0.057)。revert 済み。
  → 「可動域に余裕があるのに使われていない」は「拘束が邪魔している」を意味しない。
- **カリキュラム後の追加学習 (v34, +3000iter)**: **全条件で悪化**
  (0.896→0.781 / 0.639→0.598 / 0.744→0.582)。効果は 3000iter で出尽くす。

### 見立てより実測(自戒)

実験前の私の予想は2回とも外れた。
- 「(B) push カリキュラムは標準的手法レベルの根拠で空振りの可能性が高い」→ 最大の改善
- 「v33 はまだ伸びしろがある」→ 追加学習は回帰

判断は必ず `verify_policy.py` の3条件(無擾乱 / push / push+DR)実測で行うこと。


---

## ⚠️ 訂正 (2026-08-20): 本文書の survival 数値は評価窓に依存する

**本文書および T079u までに記録した survival は、すべて特定の評価窓での値**であり、
窓を変えると大きく変わる。次セッションはこの但し書き込みで読むこと。

    同じ v33 を stairs単体で評価:
        15秒窓  survival 0.730
        30秒窓  survival 0.000

平地は窓依存しない(10秒 0.984 / 20秒 0.980 / 30秒 0.965)。窓依存は**地形コースが
有限長**であることに起因する。単一地形のコースは全長3.6m程度で、指令0.27m/sなら
約13秒で走り切る。その先に地形ジオメトリは無いが terrain_dz は上限値を返し続ける。

### 影響

- **地形をまたいだ survival の比較はできない**。コース長が違えば意味が違う。
- 本文書の「corridor(0.146)は専用方策(0.744)に大きく劣る」という記述は
  **異なる窓の数値を並べた無効な比較**。両者は直接比較できない。

### 代わりに使う指標

**踏破率 = 到達prog / コース全長**。コース長にも窓長にも依存しない。

    階段昇段 v33: 踏破率 平均96.0% / 中央100% / 完走89.1%
    (従来の survival 0.744(15秒)・height_gained +1.075m と整合)

実装は `eval_course_completion_WIP.py`。**ただし未完成**: stairs_down で 14.8% という、
検証済みの travel +3.947m・フレーム目視での降段確認と矛盾する値を返す。
原因未特定(スポーン原点は prog 0 で正常であることは確認済み)。**使用前に要修正**。

### 次セッションの最優先事項

1. `eval_course_completion_WIP.py` の降り系地形での不具合を特定・修正する
2. 全 known_good チェックポイントを踏破率で測り直す
3. 本文書と trouble_history の窓依存の数値を訂正する

### corridor の現状(踏破率での再評価が必要)

corridor は3回試行(v40 カリキュラム無し / v41 段数カリキュラム / v42 スポーン
カリキュラム)。いずれも 30秒窓 survival 0.14-0.19。診断で判明した分布:

    stairs_up 区間(2.0-5.0m)で終了  66.2 %   <- 最大の脱落点
    5.0m を越えられた割合            30.9 %

なお corridor と単一地形で **terrain_dz の高さプロファイルは一致**している
(境界を除く57点で完全一致)。height-scan の前方プロファイルも一致。
報酬(naturalnessマスク)の差は評価には効かない。**同じ方策が両者で違う結果になる
理由は未解明**であり、窓依存を除いた再評価が先。
