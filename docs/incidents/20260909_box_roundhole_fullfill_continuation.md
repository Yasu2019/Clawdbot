# Full-fill continuation check and Telegram caption correction

The run was continued from 5 s to 8 s with the same gate/vent case. It ended
normally, but mean `alpha.polymer` plateaued at 0.86865901 rather than reaching
100%. This proves that extending time alone does not fix the short shot; the
remaining air/flow boundary condition must be corrected (vent connectivity,
vent area/position, or injection/pressure specification).

The Telegram notification was resent using ASCII-only caption text after the
Japanese caption displayed as question marks. The video remains visual-QC
checked, but it is explicitly labelled partial fill and `PROXY_ONLY`.
