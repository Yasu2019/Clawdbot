# CalculiX thermal-shrinkage reanalysis

- Source: OpenFOAM cooling temperature field mapped into the existing box mesh
- Material: virtual CTE `1.0e-4 1/K`, `E=1.2e9 Pa`, `ν=0.42`
- Packing pressure metadata: `30 MPa`
- Solver: CalculiX 2.16 in container
- Result: **PASS**; `.frd`, `.dat`, and `.sta` were produced and the log ends
  with `Job finished`

The input deck is generated by `scripts/run_calculix_shrinkage_reanalysis.py`.
Replace the virtual CTE, elastic constants, cooling field, and mapped pressure
with measured/calibrated fields for production use.
